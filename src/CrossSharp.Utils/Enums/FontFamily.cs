namespace CrossSharp.Utils.Enums;

public enum FontFamily
{
    // Think what should happen if user selects the font he doesn't have installed
    Default,
    DejaVuSans, // Linux
    Corbel, // Windows
}
